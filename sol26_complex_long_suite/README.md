# SOL26 Long Complex Suite

This bundle contains 8 long valid SOL26 programs in `.test` format.
The scenarios intentionally combine multiple mechanisms in each program:
closures, returned blocks, `whileTrue:`, `timesRepeat:`, `from:`,
string operations, boolean short-circuiting, inheritance, and `self`/`super`.
